---
description: 每次版本更新后提交并推送到 GitHub Pages
alwaysApply: true
---

# 发布到 GitHub

完成会改变游戏内容、界面或缓存版本的改动后，默认执行：

1. 提高 `sw.js` 的 `VERSION`，并同步 `index.html` 的 `?v=` 缓存标记
2. 创建 git commit
3. `git push` 到 origin，触发 GitHub Pages

不要询问是否发布。
